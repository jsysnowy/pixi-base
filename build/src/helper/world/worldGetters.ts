namespace com.sideplay.helper.world {
	export var center: PIXI.Point = new PIXI.Point(config.settings.nativeWidth / 2, config.settings.nativeHeight / 2);
	export var centerX: number = config.settings.nativeWidth / 2;
	export var centerY: number = config.settings.nativeHeight / 2;
	export var width: number = config.settings.nativeWidth;
	export var height: number = config.settings.nativeHeight;
}
